    import XCTest
    @testable import DTPermissionKit

    final class DTPermissionKitTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(DTPermissionKit().text, "Hello, World!")
        }
    }
